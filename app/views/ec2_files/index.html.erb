<h1>Server Files</h1>
<p>Displays files and directories located in <i>/var/files/</i></p>
<p>Notice this is meant to contain temp files that can be deleted at any time.</p>
<ul class="file_list">
  <% if @server.get_local_files.nil? %>
      <h2>Server must be on to view files.</h2>
  <% else %>
      <%= render 'files/list', :file => @server.get_local_files, :project => @server %>
  <% end %>
</ul>
<br/>