<%= simple_form_for [@project, @project_file], :html => {:multipart => true} do |f| %>

  <div class="form-inputs">
    <%= f.file_field :file %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
