<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "WCAI" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <%= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <%= link_to "WCAI", root_path, :class => "brand" %>
          <div class="container nav-collapse">
            <ul class="nav">
              <li><%= link_to "View Projects", projects_path  %></li>
              <li><%= link_to "About WCAI", about_path  %></li>
              <li><%= link_to "Contact Us", contact_us_path  %></li>
              
            </ul>
            <% if user_signed_in? %>
            <ul class="nav pull-right">
              <li class="nav pull-right"><%= link_to 'Logout', destroy_user_session_path, :method => :delete %></li>
            
            </ul>
            <% end %>
          </div><!--/.nav-collapse -->
          </div>
        </div>
      </div>
    </div>

    <div class="container">
        <div class="row">
          <div class="span9">
             <%= yield %>
          </div>
         
          <div class="span3">
            <div class="well sidebar-nav">
              <!-- <h3>Shortcuts</h3> -->
              <ul class="nav nav-list">
              <% if user_signed_in? %>
                    <% if current_user.is? :admin %>
                 <li class="nav-header">View</li>
                <li><%= link_to "Projects", projects_path  %></li>
                <li><%= link_to "Instances", servers_path %></li>
              </br>
                <li class="nav-header">Create</li>
                <li><%= link_to "Project", new_project_path  %></li>
                <li><%= link_to "Users", new_batch_users_path  %></li>
                 <% end %>
                <% else %>
                <li class="nav-header">Login</li>
                <li><%= link_to "Sign In", new_user_session_path  %></li>
                <% end %>
              </ul>
            </div><!--/.well -->
          </div><!--/span-->
        </div><!--/row-->

      <footer>
        <p>&copy; WCAI 2012</p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
