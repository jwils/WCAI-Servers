<h1>Listing connections</h1>

<table>
  <tr>
    <th>Sql user</th>
    <th>Sql password</th>
    <th>References</th>
    <th>References</th>
    <th>Connection open</th>
    <th>Connection closed</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @connections.each do |connection| %>
  <tr>
    <td><%= connection.sql_user %></td>
    <td><%= connection.sql_password %></td>
    <td><%= connection.references %></td>
    <td><%= connection.references %></td>
    <td><%= connection.connection_open %></td>
    <td><%= connection.connection_closed %></td>
    <td><%= link_to 'Show', connection %></td>
    <td><%= link_to 'Edit', edit_connection_path(connection) %></td>
    <td><%= link_to 'Destroy', connection, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Connection', new_connection_path %>
